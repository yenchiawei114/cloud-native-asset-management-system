apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: asset-mgmt
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "8000"
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
